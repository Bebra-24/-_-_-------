<Window x:Class="WpfApp1.RequestWorkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Работа с заявкой" Height="600" Width="700"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1a237e" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <TextBlock x:Name="TitleText" Text="Информация о заявке" FontSize="18" FontWeight="Bold" Foreground="White"/>
        </Border>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <!-- Основная информация -->
                <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,5" Background="#FAFAFA">
                    <StackPanel>
                        <TextBlock Text="Информация о заявке" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Тип:" Margin="5" Foreground="#555555"/>
                            <TextBlock x:Name="TypeText" Grid.Column="1" Margin="5"/>

                            <TextBlock Grid.Column="0" Text="Цель:" Margin="5" Foreground="#555555" Grid.Row="1"/>
                            <TextBlock x:Name="PurposeText" Grid.Column="1" Margin="5" Grid.Row="1" TextWrapping="Wrap"/>

                            <TextBlock Grid.Column="0" Text="Дата посещения:" Margin="5" Foreground="#555555" Grid.Row="2"/>
                            <TextBlock x:Name="DateText" Grid.Column="1" Margin="5" Grid.Row="2"/>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Список посетителей (для групповых заявок) -->
                <Border x:Name="VisitorsBorder" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,5" Background="#FAFAFA" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="Список посетителей" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
                        <ListBox x:Name="VisitorsList" Height="150" SelectionChanged="VisitorsList_SelectionChanged">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" FontSize="13" Padding="5">
                                        <TextBlock.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="Добавить в черный список" Click="AddToBlacklist_Click"/>
                                            </ContextMenu>
                                        </TextBlock.ContextMenu>
                                    </TextBlock>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </Border>

                <!-- Фиксация времени -->
                <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,5" Background="#FAFAFA">
                    <StackPanel>
                        <TextBlock Text="Фиксация времени" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Column="0" Text="Время входа:" Margin="5" Foreground="#555555"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="5">
                                <TextBlock x:Name="EntryTimeText" Text="Не зафиксировано" Width="200" VerticalAlignment="Center" FontWeight="Bold"/>
                                <Button x:Name="EntryTimeBtn" Content="Зафиксировать вход" Width="150" Margin="10,0,0,0" 
                                        Click="EntryTimeBtn_Click" Background="#4CAF50" Foreground="White"/>
                            </StackPanel>

                            <TextBlock Grid.Column="0" Text="Время выхода:" Margin="5" Foreground="#555555" Grid.Row="1"/>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="5" Grid.Row="1">
                                <TextBlock x:Name="ExitTimeText" Text="Не зафиксировано" Width="200" VerticalAlignment="Center" FontWeight="Bold"/>
                                <Button x:Name="ExitTimeBtn" Content="Зафиксировать выход" Width="150" Margin="10,0,0,0" 
                                        Click="ExitTimeBtn_Click" Background="#FF9800" Foreground="White"/>
                            </StackPanel>
                        </Grid>

                        <!-- Информация о нарушении -->
                        <Border x:Name="ViolationBorder" CornerRadius="5" Padding="10" Margin="0,10,0,0" Background="#FFEBEE" Visibility="Collapsed">
                            <StackPanel>
                                <TextBlock Text="⚠️ ВНИМАНИЕ: Нарушение временных параметров!" 
                                           Foreground="#C62828" FontWeight="Bold" FontSize="12" TextWrapping="Wrap"/>
                                <TextBlock x:Name="ViolationText" Text="" Foreground="#C62828" FontSize="11" Margin="0,5,0,0" TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15">
            <Button Content="Закрыть" Width="100" Height="40" Margin="5" 
                    Background="#2196F3" Foreground="White" Click="CloseBtn_Click"/>
        </StackPanel>
    </Grid>
</Window>